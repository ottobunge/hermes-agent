name: session-bridge
version: 0.99.0-deprecated
kind: standalone
description: >
  DEPRECATED -- see plugins/session_bus/. This plugin has been merged
  into session-bus; the directory remains as a deprecation shim for one
  minor cycle. Tools renamed (session_emit -> bus_emit, etc.) -- see
  plugins/session_bus/README.md.
provides_tools: []
hooks: []
